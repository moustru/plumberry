import { css } from "@/styled-system/css";

export const buttonStyles = css({
  paddingBlock: "0.625rem",
  paddingInline: "1rem",
  borderRadius: "10px",
});

export const blackVariant = css({
  bgColor: "#000",
  color: "#fff",
});

export const whiteVariant = css({
  bgColor: "#fff",
  color: "#000",
});

export const transparentVariant = css({
  bgColor: "transparent",
  color: "#000",
});
