import { css } from "@/styled-system/css";

export const managementBlock = css({
  paddingBlock: "6rem",
  paddingInline: "6.5rem",
});

export const managementItemBlock = css({
  display: "flex",
  gap: "1.25rem",
  marginBlockStart: "3.75rem",
});
