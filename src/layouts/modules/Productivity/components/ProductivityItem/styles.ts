import { css } from "@/styled-system/css";

export const productivityItemStyles = css({
  display: "flex",
  flexDirection: "column",
  gap: "1.25rem",
});

export const productivityItemIcon = css({
  width: "1.5rem",
  height: "1.5rem",
});
