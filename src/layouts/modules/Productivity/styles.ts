import { css } from "@/styled-system/css";

export const productivityBlock = css({
  position: "relative",
  paddingBlock: "7rem",
  paddingInline: "3rem",
  bgGradient: "to-b",
  gradientFrom: "#fff",
  gradientTo: "#D2DCFF",
});

export const pyramidStyles = css({
  position: "absolute",
  top: "40%",
  right: 0,
});

export const torusStyles = css({
  position: "absolute",
  top: "55%",
  left: 0,
});

export const itemsBlock = css({
  display: "flex",
  gap: "1.25rem",
});
